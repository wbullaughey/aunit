with "base";
with "ada_lib";
with "root";

library project AUnit_Uninstalled is
ggg
   for Languages use ("Ada");
   for Object_Dir use Root.Object_Directory;
   for Library_Name use "aunit";
   for Library_Kind use Base.Library_Kind;
   for Library_Dir use Root.Library_Directory;
   for Source_Dirs use (".");

   package Compiler renames Root.Compiler;

-- package Install is
--    for Active use "True";
-- end Install;
--
-- for Externally_Built use "False";

end AUnit_Uninstalled;
